<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.MyApplication" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.MyApplication.AppCompat" parent="Theme.AppCompat.Light.NoActionBar">

    </style>

    <style name="AppTheme.Light" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Definicje kolorów dla trybu jasnego -->
        <item name="android:textColor">@color/black</item>
        <item name="android:background">@color/background</item>
        <item name="android:textColorHint">@color/black</item>
        <item name="tabTextColor">@android:color/black</item>



        <!-- Inne właściwości stylu -->
    </style>

    <style name="AppTheme.Dark" parent="Theme.AppCompat.NoActionBar">
        <!-- Definicje kolorów dla trybu ciemnego -->
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@color/black</item>
        <item name="android:textColorHint">@color/white</item>
        <item name="tabTextColor">@android:color/white</item>

        <!-- Inne właściwości stylu -->
    </style>

</resources>